{
  "name": "slate",
  "private": true,
  "description": "Serverless dependencies for the /api functions. The app itself has no build step.",
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "web-push": "^3.6.7"
  }
}
